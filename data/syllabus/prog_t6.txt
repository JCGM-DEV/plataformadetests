tema 6
¿Cuál es la diferencia fundamental de diseño entre una Clase Abstracta y una Interfaz?
*La Clase Abstracta define qué "ES" el objeto (identidad), mientras que la Interfaz define qué "PUEDE HACER" (capacidad).
Las Interfaces permiten guardar variables de estado mutables y las Clases Abstractas no.
Las Clases Abstractas permiten herencia múltiple y las Interfaces solo herencia simple.
Mediante una clase abstracta con métodos estáticos, aunque no es correcto.
EXPL:Relaciones entre Clases y Herencia. Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corresponden a lo preguntado.
¿Qué característica define al modificador de acceso protected?
Hace que el miembro sea visible solo para la clase actual y nadie más., lo cual no es correcto.
*Permite el acceso a cualquier clase del mismo paquete y a cualquier subclase, incluso en paquetes distintos.
Es exactamente igual al acceso public, pero solo funciona con métodos static.
Con un certificado SSL autofirmado, aunque no es la respuesta correcta.
EXPL:La respuesta correcta es: Permite el acceso a cualquier clase del mismo paquete y a cualquier subclase, incluso en paquetes distintos.. Las demás opciones no corresponden a la definición o comportamiento correcto en este contexto.
En Java, ¿cuál es la jerarquía máxima de la que heredan implícitamente todas las clases, incluidos los arrays y Strings?
*La clase java.lang.Object.
La clase Super.
No existe una jerarquía común; cada clase es independiente a menos que use extends.
Mediante el protocolo OSPF de enrutamiento dinámico, aunque no corresponde.
EXPL:Esto significa que TODAS las clases en Java (arrays y Strings incluidos) forman una única jerarquía gigante con Object en la cúspide. Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corresponden a lo preguntado.
¿Por qué Java prohíbe la herencia múltiple de clases (heredar de dos padres a la vez)?
*Para evitar el "Problema del Diamante" y la ambigüedad en la ejecución de métodos.
Porque consumiría demasiada memoria en el Heap al duplicar los constructores.
Porque todas las clases deben ser obligatoriamente final para ser seguras.
Mediante el protocolo OSPF de enrutamiento dinámico, aunque no corresponde.
EXPL:La "Herencia Múltiple" de Java: Java no permite heredar de dos padres (extends A, B es ilegal). Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corresponden a lo preguntado.
¿Cuál es la función principal de la anotación @Override en la sobreescritura de métodos?
Obligar al método a ser final para que no pueda volver a cambiarse.
*Actuar como un control de calidad que avisa al compilador si el método no existe en el padre.
Permitir que el método cambie su lista de parámetros (Sobrecarga).
Mediante el protocolo OSPF de enrutamiento dinámico, aunque no corresponde.
EXPL:Sobreescritura de métodos (Overriding) La sobreescritura (o overriding) ocurre cuando una subclase (hija) proporciona una implementación específica de un método que ya está definido en su superclase (padre). Las otras opciones son incorrectas porque describen conceptos diferentes o características q
Sobre las clases abstractas, ¿cuál es la "Regla de Hierro"?
Deben tener obligatoriamente todos sus métodos vacíos.
*No se pueden crear objetos (instancias) de una clase abstracta.
Solo pueden heredar de interfaces y nunca de otras clases.
Mediante una clase abstracta con métodos estáticos, aunque no es correcto.
EXPL:La respuesta correcta es: No se pueden crear objetos (instancias) de una clase abstracta.. Las demás opciones no corresponden a la definición o comportamiento correcto en este contexto.
Si deseamos garantizar que un algoritmo crítico no sea alterado por ninguna subclase, ¿qué debemos hacer?
Declarar el método como abstract.
*Declarar el método como final.
Declarar el método como static y private simultáneamente.
Mediante una clase abstracta con métodos estáticos, aunque no es correcto.
EXPL:La respuesta correcta es: Declarar el método como final.. Las demás opciones no corresponden a la definición o comportamiento correcto en este contexto.
Al decidir entre Herencia y Composición, ¿qué principio de diseño de software recomienda el temario?
Priorizar siempre la Herencia para maximizar la reutilización de código.
*Favorecer la Composición sobre la Herencia para obtener un acoplamiento más bajo y flexible.
Usar la Herencia para relaciones "Tiene-Un" y la Composición para relaciones "Es-Un".
Mediante el protocolo OSPF de enrutamiento dinámico, aunque no corresponde.
EXPL:Principio de Diseño de Software: "Favorece la Composición sobre la Herencia". Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corresponden a lo preguntado.
Si un método es declarado como static, ¿cuál de las siguientes afirmaciones es CORRECTA?
*Pertenece a la clase y no puede acceder a variables de instancia ni usar this.
Requiere crear un objeto con new antes de poder ser invocado.
Cada objeto creado tiene su propia copia privada de dicho método en memoria.
Mediante una clase abstracta con métodos estáticos, aunque no es correcto.
EXPL:La respuesta correcta es: Pertenece a la clase y no puede acceder a variables de instancia ni usar this.. Las demás opciones no corresponden a la definición o comportamiento correcto en este contexto.
Respecto a los constructores y la herencia, ¿cuál es el orden estricto de inicialización?
Primero se inicializa la subclase (hija) y finalmente la superclase (padre).
Se inicializan simultáneamente para optimizar la carga de la JVM.
*Los padres siempre se inicializan antes que los hijos, empezando desde la clase Object.
Mediante una clase abstracta con métodos estáticos, aunque no es correcto.
EXPL:El Orden de Inicialización Cuando haces new Coche(), podrías pensar que solo se ejecuta el constructor de Coche. Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corresponden a lo preguntado.
