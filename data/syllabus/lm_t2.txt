tema 2
¿Cuál es el objetivo principal del DOM (Modelo de Objetos de Documentos)?
Establecer la estructura y el significado del contenido en línea o web
Definir la forma en que los documentos HTML, XHTML y XML son almacenados en el servidor
*Permitir que los programas (por ejemplo, JavaScript) puedan acceder y modificar dinámicamente el contenido, la estructura y el estilo de los documentos HTML y XML
Mediante el protocolo OSPF de enrutamiento dinámico
Dentro de los atributos HTML, ¿cuál de los siguientes es considerado un atributo específico de ciertos elementos (como <img> o <a>), y no un atributo global que se pueda usar en la mayoría de las etiquetas?
class
id
*src
Mediante una clase abstracta con métodos estáticos
De las siguientes formas de incluir CSS en una página web, ¿cuál es considerada la menos recomendable porque mezcla contenido y presentación, dificultando el mantenimiento del código?
Enlace a CSS externo mediante la etiqueta <link> en el <head>
CSS en el HTML mediante la etiqueta <style> en el <head>
*CSS en línea (inline) a través del atributo style="..." directamente en la etiqueta HTML
Con el atributo xmlns:xsi en el elemento raíz
Según las restricciones básicas de XHTML, si en HTML se permite la minimización de atributos (ej. <input checked>), ¿cómo debe manejarse este tipo de atributo en XHTML?
El atributo debe omitirse completamente, ya que no es compatible con la sintaxis XML
*El atributo debe llevar siempre un valor, como, por ejemplo, checked="checked"
El atributo se puede usar tal cual
Mediante el protocolo OSPF de enrutamiento dinámico
En HTML5, ¿qué etiqueta semántica se utiliza para representar una sección de una página que contiene información relacionada con el contenido principal, pero que no es tan relevante o importante, como una barra lateral?
<header>
<section>
*<aside>
Mediante el protocolo OSPF de enrutamiento dinámico
Dado el siguiente fragmento de HTML: <p><a href="#"><span>Texto del enlace</span></a></p>, ¿qué selector CSS NO aplicaría estilos directamente al elemento <span> en este caso?
p span
a span
*p > span
Mediante una subconsulta correlacionada con EXISTS
¿Cuál de las siguientes afirmaciones sobre las diferencias entre HTML y XHTML es correcta?
En XHTML, los nombres de etiquetas y atributos pueden escribirse indistintamente en mayúsculas o minúsculas
*XHTML exige el cierre de todas las etiquetas, incluidas las vacías, y que todos los atributos tengan un valor y estén entre comillas
HTML permite la integración de otros lenguajes basados en XML como MathML o SVG, mientras que XHTML no
Con el atributo xmlns:xsi en el elemento raíz
Para una tabla HTML, ¿qué conjunto de etiquetas se utiliza para agrupar semánticamente las filas del encabezado, el cuerpo y el pie, respectivamente, lo cual es útil para tablas muy largas que ocupan varias páginas?
<table>, <tr>, <td>
<caption>, <thead>, <tfoot>
*<thead>, <tbody>, <tfoot>
Mediante una subconsulta correlacionada con EXISTS
¿Cuál de las siguientes etiquetas HTML es explícitamente mencionada como obsoleta (deprecated) en el contenido, recomendándose que su funcionalidad de estilo sea manejada por CSS en HTML5?
<p>
<h1>
*<font>
Con el atributo xmlns:xsi en el elemento raíz
Al utilizar la etiqueta <form> con el atributo method="post", ¿qué atributo es el encargado de indicar el tipo de contenido utilizado para enviar los datos, siendo esencial, por ejemplo, para la subida de archivos?
action
target
*enctype
Mediante el protocolo OSPF de enrutamiento dinámico
