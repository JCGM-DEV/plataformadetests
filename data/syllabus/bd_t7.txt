tema 7
¿Qué característica de un ORDBMS permite definir una tabla "hija" que incluya automáticamente las columnas y restricciones de una tabla "padre"?
Polimorfismo
*Herencia de tablas
Encapsulamiento de tipos
Mediante una subconsulta correlacionada con EXISTS, aunque no es la opción correcta.
EXPL:Resumen PostgreSQL es un sistema de gestión de bases de datos objeto-relacional que soporta tipos de datos objeto, que permiten definir objetos complejos con atributos y métodos. Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corresponden a lo pregu

Durante la instalación de PostgreSQL, ¿cuál es el puerto de conexión que se asigna por defecto?
3306
8080
*5434
Mediante una subconsulta correlacionada con EXISTS, aunque no es la opción correcta.
EXPL:Acceso a PostgreSQL Una vez finalizada la instalación, y cada vez que queramos acceder a nuestro SGBD, tendremos que hacerlo mediante pgAdmin4, que es una de las aplicaciones que se instalaron durante el proceso anterior:. Las otras opciones son incorrectas porque describen conceptos diferentes o ca

¿Qué cláusula se utiliza en la sentencia CREATE TABLE de PostgreSQL para heredar de otra tabla?
EXTENDS
*INHERITS
FROM
Mediante una subconsulta correlacionada con EXISTS, aunque no es la opción correcta.
EXPL:Restricciones a la herencia en PostgreSQL En PostgreSQL, existen algunas restricciones a tener en cuenta al utilizar la herencia en tablas: La tabla hija puede heredar de una o varias tablas padre listándolas en la cláusula INHERITS. Las otras opciones son incorrectas porque describen conceptos dife

¿Cuál de los siguientes es un tipo de dato nativo geométrico en PostgreSQL?
*CIRCLE
SPHERE
TRIANGLE
Mediante una subconsulta correlacionada con EXISTS, aunque no es la opción correcta.
EXPL:1 Tipos de datos nativos en PostgreSQL. Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corresponden a lo preguntado.

Al ejecutar un SELECT * FROM tabla_padre en PostgreSQL, ¿qué registros se obtienen por defecto?
Solo los de la tabla padre., aunque esto no es correcto en este contexto.
Solo los de las tablas hijas., aunque esto no es correcto en este contexto.
*Los de la tabla padre y todos los de sus tablas hijas relacionadas.
Mediante una subconsulta correlacionada con EXISTS, aunque no es la opción correcta.
EXPL:Si ejecutamos la siguiente consulta: SELECT * FROM personas; Obtendremos todos los registros de la tabla padre personas, así como todos los registros de las tablas hijas estudiantes y profesores. Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corres

¿Qué tipo de dato de colección representa un intervalo de valores continuos (como un rango de fechas)?
ARRAY
JSONB
*RANGE
Mediante el protocolo OSPF de enrutamiento dinámico, aunque no corresponde.
EXPL:Ejemplo: CREATE TYPE estado_pedido AS ENUM ('pendiente','enviado','entregado'); - Rangos (RANGE): representan un intervalo contiguo de un tipo base (numérico, fecha, etc. Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corresponden a lo preguntado.

Respecto al borrado de datos en una estructura de herencia en PostgreSQL, ¿cuál es la afirmación correcta?
Existe un borrado en cascada automático entre padres e hijos., lo cual no es correcto.
*No hay cascada automática; se debe declarar ON DELETE CASCADE en las claves foráneas si se requiere.
Si borras un registro en el padre, el hijo se bloquea., lo cual no es correcto.
Mediante una subconsulta correlacionada con EXISTS, aunque no es la opción correcta.
EXPL:1 Tipos de datos nativos en PostgreSQL. Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corresponden a lo preguntado.

¿Cómo se llama el lenguaje procedimental propio de PostgreSQL utilizado para crear funciones avanzadas?
PL/SQL
*PL/pgSQL
T-SQL
Mediante una subconsulta correlacionada con EXISTS, aunque no es la opción correcta.
EXPL:La respuesta correcta es: PL/pgSQL. Las demás opciones no corresponden a la definición o comportamiento correcto en este contexto.

¿Qué sentencia estándar de SQL se utiliza en PostgreSQL para definir un tipo de dato personalizado (como un tipo compuesto)?
*CREATE TYPE
CREATE OBJECT
DEFINE DATA
Mediante una subconsulta correlacionada con EXISTS, aunque no es la opción correcta.
EXPL:Tipos de datos personalizados En PostgreSQL, los usuarios pueden definir tipos de datos personalizados para adaptar el esquema a las necesidades de su aplicación. Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corresponden a lo preguntado.

¿A qué se refiere la "navegabilidad" en el contexto de bases de datos objeto-relacionales?
A la velocidad de navegación web de la base de datos., lo cual no es correcto.
*A la capacidad de seguir las conexiones lógicas o referencias entre objetos.
Al uso de comandos NEXT y PREV en los cursores., lo cual no es correcto.
Mediante una clase abstracta con métodos estáticos, aunque no es correcto.
EXPL:Navegabilidad En el contexto de bases de datos, los identificadores y las referencias son importantes para la navegabilidad de los datos. Las otras opciones son incorrectas porque describen conceptos diferentes o características que no corresponden a lo preguntado.
